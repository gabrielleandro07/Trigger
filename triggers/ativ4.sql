CREATE DATABASE ativ4;

USE ativ4;

CREATE TABLE CAIXA
(
DATA_TRANSACAO DATETIME,
SALDO_INICIAL DECIMAL(10,2),
SALDO_FINAL DECIMAL(10,2)
);


INSERT INTO CAIXA VALUES (now(), 100, 100);

SELECT * FROM CAIXA;


CREATE TABLE VENDAS
(
DATA_TRANSACAO DATETIME,
CODIGO INT,
VALOR DECIMAL(10,2)
);

SELECT * FROM VENDAS;


INSERT INTO VENDAS VALUES (now(), 1, 10);
INSERT INTO VENDAS VALUES (now(), 2, 10);
INSERT INTO VENDAS VALUES (now(), 3, 10);


DELETE FROM VENDAS WHERE CODIGO = 1;








